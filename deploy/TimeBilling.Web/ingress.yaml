apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: timebilling
  annotations:
    ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
  - host: "timebilling.local"
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: timebilling
            port:
              number: 80
